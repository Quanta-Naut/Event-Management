# Database Connection String (Required)
# PostgreSQL connection string format: postgresql://username:password@hostname:port/database_name
# Example for local development with Docker:
DATABASE_URL=postgresql://admin:password@localhost:5432/eventforge

# JWT Secret for Authentication (Required)
# This should be a secure random string in production environments
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-secret-key-here

# Node Environment
# Options: development, production, test
NODE_ENV=development

# Server Port (Optional - defaults to 5000)
PORT=5000

# Logging Level (Optional)
# Options: error, warn, info, http, verbose, debug, silly
LOG_LEVEL=info